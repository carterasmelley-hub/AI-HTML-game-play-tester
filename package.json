{
  "name": "arcade-grid",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "GitHub Pages-ready portal for HTML games",
  "scripts": {
    "build": "npm run generate",
    "generate": "node scripts/generate-games-manifest.mjs",
    "dev": "node server.js",
    "setup:playwright": "playwright install chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "playwright": "^1.58.2"
  }
}
